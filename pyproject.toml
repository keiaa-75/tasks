[project]
name = "tasks"
version = "0.1.0"
description = "A Google Tasks client for Linux."
requires-python = ">=3.8"
dependencies = [
    "pyqt6",
    "google-api-python-client",
    "google-auth-httplib2",
    "google-auth-oauthlib",
    "tenacity",
]

[project.scripts]
tasks = "tasks.main:main"